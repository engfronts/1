// Reviewer credential hashes are intentionally omitted from the public build.
// The editorial office distributes reviewer IDs and hashed passwords via a private channel.
// To enable local validation, drop the secure dataset here in the same format: [{ username, hash }].
window.EF_REVIEWER_HASHES = [];
